project = "wonderful-reprieve"
environment = "production"

[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run build && cd backend && npm install && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
